{
  "name": "crypto-index",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "server": "concurrently \"tsc -p server && node dist/server/app.js\"",
    "serverdev": "concurrently \"tsc -w -p server\" \"nodemon dist/server/app.js\""
  },
  "private": true,
  "dependencies": {
    "axios": "^0.17.1",
    "node-telegram-bot-api": "^0.30.0",
    "nodemon": "^1.14.8"
  },
  "devDependencies": {
    "concurrently": "^3.5.1",
    "ts-node": "~3.0.4",
    "typescript": "~2.3.3"
  }
}
